{
  "openapi": "3.0.0",
  "info": { "title": "Path Metadata Fixture", "version": "1.0.0" },
  "paths": {
    "/widgets/{id}": {
      "summary": "Widget resource",
      "description": "Operations on a single widget",
      "parameters": [
        { "name": "id", "in": "path", "required": true, "schema": { "type": "string" } }
      ],
      "get": {
        "responses": { "200": { "description": "ok" } }
      },
      "delete": {
        "responses": { "204": { "description": "deleted" } }
      }
    }
  }
}
